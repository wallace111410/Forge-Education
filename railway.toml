[build]
builder = "NIXPACKS"
buildCommand = "corepack enable && corepack prepare pnpm@10.0.0 --activate && pnpm install --no-frozen-lockfile"

[deploy]
startCommand = "node artifacts/forge-education/server/index.js"
restartPolicyType = "ON_FAILURE"
