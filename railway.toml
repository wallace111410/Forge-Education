[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm && pnpm install --no-frozen-lockfile"

[deploy]
startCommand = "node artifacts/forge-education/server/index.js"
restartPolicyType = "ON_FAILURE"
